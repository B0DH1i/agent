

# Core FastAPI and server dependencies (DAWOS compatible)
fastapi==0.116.1
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-multipart==0.0.20

# HTTP client for DAWOS integration (DAWOS compatible)
httpx==0.28.1
requests==2.32.5

# Environment and configuration (DAWOS compatible)
python-dotenv==1.1.1

# Security (DAWOS compatible)
python-jose[cryptography]==3.5.0

# AI and ML dependencies (Agent specific - MINIMAL)
groq==1.0.0

# Vector Database and Embeddings (Agent specific)
chromadb>=0.4.15
sentence-transformers>=2.2.0
numpy>=1.21.0
setuptools>=65.0.0